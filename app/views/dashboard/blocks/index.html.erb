<table class="table table-hover table-condensed">
  <thead>
  <tr>
    <th><%= t(:title_label) %></th>
    <th colspan="3"></th>
  </tr>
  </thead>
  <tbody>
  <% @blocks.each do |block| %>
    <tr class="table table-hover table-condensed">
      <td><%= block.title %></td>

      <% if block.current? %>
        <td>
          <%= link_to t(:set_as_common_label), reset_as_current_block_path(block),
                      method: :put %>
        </td>
      <% else %>
        <td>
          <%= link_to t(:set_as_current_label), set_as_current_block_path(block),
                      method: :put %>
        </td>
      <% end %>
      <td><%= link_to t(:edit_label), edit_block_path(block) %></td>
      <td><%= link_to t(:delete_label), block, method: :delete,
                      data: { confirm: t(:confirm_deletion) } %></td>
    </tr>
  <% end %>
  </tbody>
</table>
